version: '3.7'
services:

  livechat_api:
    build: ./
    ports:
      - 8000:80
    depends_on:
      - mongodb

  mongodb:
    restart: always
    image: mongo:latest
    ports:
      - 27017:27017

  # mongo_seed:
  #   image: mongo:latest
  #   links:
  #     - mongodb
  #   volumes:
  #     - ./data/mongo-seed:/mongo-seed/
  #   command: /mongo-seed/mongoimport.sh
